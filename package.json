{
  "name": "parcel-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel src/*.html -d public",
    "watch": "parcel watch src/*.html --no-cache -d publid",
    "build": "parcel build src/*.html --no-cache -d public"
  },
  "author": "Syohei Yamaki",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^9.6.1",
    "css-mqpacker": "^7.0.0",
    "node-sass": "^7.0.0",
    "parcel-bundler": "^1.12.3"
  },
  "browserslist": [
    "last 2 versions"
  ]
}
